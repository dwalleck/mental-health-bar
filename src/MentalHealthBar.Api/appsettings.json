{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=mentalhealthbar;Username=postgres;Password=postgres"
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5000",
      "https://localhost:5001"
    ]
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // AllowedHosts: "*" is acceptable for local development
  // PRODUCTION: Must be restricted to specific domains in appsettings.Production.json
  // to prevent host header injection attacks
  "AllowedHosts": "*"
}
